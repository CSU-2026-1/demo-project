global:
  scrape_interval: 5s

scrape_configs:
  - job_name: nginx-load-balancer
    static_configs:
      - targets:
          - nginx-exporter:9113

  - job_name: api-replicas
    dns_sd_configs:
      - names:
          - api
        type: A
        port: 8000
    metrics_path: /metrics
